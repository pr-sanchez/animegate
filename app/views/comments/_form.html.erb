  <% if user_signed_in? %>


<%= form_for ([@post, @post.comments.build]) do |f| %><br>
<p>
	<%= f.label :Nombre%><br>
	<%= f.text_field :name %>
</p>

<p>
	<%= f.label :Comentario%><br>
	<%= f.text_area :body %>
</p>
<br>
<p><br>
	<%= f.submit%>
</p>

<% end %>
<%else %>

<p>Necesitas Iniciar sesión para poder hacer comentarios</p> 
<%= link_to "Iniciar sesión", new_user_session_path, class: "btn btn-default"%>
<br><br>
<% end %>